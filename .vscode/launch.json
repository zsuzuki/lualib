{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceRoot}/luatest",
            "args": [
                "scripts/test.lua"
            ],
            "cwd": "${workspaceRoot}"
        }
    ]
}